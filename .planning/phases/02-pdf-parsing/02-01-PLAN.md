---
phase: 02-pdf-parsing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  ['package.json', 'src/parsers/pdf-parser.ts', 'src/parsers/index.ts', 'src/types/pdf.ts']
autonomous: true

must_haves:
  truths:
    - 'System can parse BNP PDF and extract raw text content'
    - 'PDF parsing library is installed and configured'
    - 'Parser returns structured text with page information'
  artifacts:
    - path: 'src/parsers/pdf-parser.ts'
      provides: 'PDF text extraction with page handling'
      exports: ['parsePdf', 'PdfContent']
    - path: 'src/types/pdf.ts'
      provides: 'PDF content type definitions'
      exports: ['PdfContent', 'PdfPage']
    - path: 'src/parsers/index.ts'
      provides: 'Parser module exports'
  key_links:
    - from: 'parsePdf function'
      to: 'pdf-parse library'
      via: 'import and call'
      pattern: "pdfParse\\(buffer\\)"
---

<objective>
Install pdf-parse library and create foundational PDF parsing infrastructure to extract raw text from BNP Paribas Fortis PDF statements.

Purpose: Establish the core PDF parsing capability that all downstream phases depend on. Without this, we cannot extract transactions.
Output: Working PDF parser that returns structured text content with page-level information.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@package.json

## Phase 2 Context

**Goal:** System can extract raw text and structure from BNP PDFs

**Success Criteria:**

1. System can parse single-page BNP PDF and extract raw text content
2. System handles multi-page PDFs without losing transaction data
3. System extracts complete card number from PDF header ("Numéro de carte XXXX")

**Requirements to cover:**

- PARSE-01: Parse BNP Paribas Fortis PDF credit card statements
- PARSE-03: Handle multi-page PDFs

**Library Selection (from STATE.md):**

- **PDF Parsing:** pdf-parse (npm)

**Existing Code:**

- TypeScript project initialized with commander
- Source files in src/ directory
- Build system working (npm run build)
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Install pdf-parse dependency</name>
  <files>package.json</files>
  <action>
    Install pdf-parse and its types:
    - Run: `npm install pdf-parse`
    - Run: `npm install --save-dev @types/pdf-parse` (if available, or use @types/pdf-parse-community)
    
    Note: pdf-parse is a popular library for extracting text from PDFs. It returns text content and metadata.
    
    After installation, verify:
    - package.json should show "pdf-parse" in dependencies
    - node_modules/pdf-parse should exist
  </action>
  <verify>cat package.json | grep "pdf-parse" && ls node_modules/pdf-parse</verify>
  <done>pdf-parse is installed and package.json updated</done>
</task>

<task type="auto">
  <name>Task 2: Create PDF types</name>
  <files>src/types/pdf.ts</files>
  <action>
    Create type definitions for PDF content structure:
    
    Define interfaces:
    - PdfPage: { pageNumber: number; text: string }
    - PdfContent: { 
        text: string;           // Full concatenated text
        pages: PdfPage[];       // Per-page text for multi-page support
        numPages: number;
        info?: any;             // PDF metadata
      }
    
    Export all types from this file.
    
    This structure supports PARSE-03 (multi-page handling) by preserving page boundaries.
  </action>
  <verify>npx tsc --noEmit to check types compile correctly</verify>
  <done>src/types/pdf.ts exists with PdfContent and PdfPage types</done>
</task>

<task type="auto">
  <name>Task 3: Implement PDF parser</name>
  <files>src/parsers/pdf-parser.ts</files>
  <action>
    Create the core PDF parsing module at src/parsers/pdf-parser.ts:
    
    1. Import pdf-parse
    2. Import types from ../types/pdf.ts
    3. Export async function `parsePdf(buffer: Buffer): Promise<PdfContent>`
    
    Implementation requirements:
    - Use pdf-parse to extract text from PDF buffer
    - Return structured PdfContent with full text
    - For multi-page support: Use pdf-parse options to get page-by-page text if possible
      (pdf-parse has a pagerender callback that can capture per-page text)
    - Handle errors gracefully (throw with descriptive message)
    
    Key implementation pattern:
    ```typescript
    const options = {
      pagerender: (pageData) => {
        // Capture per-page text
        return pageData.getTextContent().then(...);
      }
    };
    ```
    
    Note: If pdf-parse doesn't directly support per-page extraction easily, 
    ensure the full text is returned and we can handle multi-page in a later iteration.
  </action>
  <verify>npm run build compiles without errors</verify>
  <done>src/parsers/pdf-parser.ts exists with working parsePdf function</done>
</task>

<task type="auto">
  <name>Task 4: Create parser index</name>
  <files>src/parsers/index.ts</files>
  <action>
    Create src/parsers/index.ts as the main entry point for the parsers module:
    
    Export:
    - All exports from ./pdf-parser
    - All exports from ../types/pdf
    
    This provides a clean import path: `import { parsePdf } from './parsers'`
  </action>
  <verify>npm run build compiles without errors</verify>
  <done>src/parsers/index.ts exists with clean exports</done>
</task>

</tasks>

<verification>
After completion:
1. `npm run build` succeeds with no TypeScript errors
2. `parsePdf` function is importable from './parsers'
3. Type definitions are complete and exported
4. Package.json includes pdf-parse dependency
</verification>

<success_criteria>

- pdf-parse library installed and listed in dependencies
- Type definitions for PDF content exist and compile
- parsePdf function can extract text from PDF buffer
- Parser module has clean exports via index.ts
- All code compiles with `npm run build`
  </success_criteria>

<output>
After completion, create `.planning/phases/02-pdf-parsing/02-01-SUMMARY.md`
</output>
