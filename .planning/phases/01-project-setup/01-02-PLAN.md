---
phase: 01-project-setup
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - package.json
  - src/index.ts
  - src/cli.ts
  - .gitignore
  - .prettierrc
  - eslint.config.mjs
autonomous: true

must_haves:
  truths:
    - Core dependencies (commander) are installed
    - CLI entry point exists and uses commander
    - npm run build compiles TypeScript without errors
    - npm run dev executes CLI in development mode
    - ESLint and Prettier are configured
    - .gitignore excludes dist/ and node_modules/
  artifacts:
    - path: src/index.ts
      provides: "Main entry point export"
      min_lines: 10
      exports: ["main function or export"]
    - path: src/cli.ts
      provides: "CLI implementation with commander"
      min_lines: 20
      contains: "import { Command } from 'commander'"
    - path: .gitignore
      provides: "Git ignore rules"
      contains: ["dist/", "node_modules/", "*.log"]
    - path: .prettierrc
      provides: "Prettier configuration"
    - path: eslint.config.mjs
      provides: "ESLint configuration"
    - path: package.json
      provides: "Updated with scripts and dependencies"
      contains: ["build", "dev", "lint", "format"]
  key_links:
    - from: package.json
      to: src/cli.ts
      via: "main field or bin script"
      pattern: 'main.*cli|bin.*cli'
    - from: src/cli.ts
      to: src/index.ts
      via: "import of main logic"
      pattern: 'import.*index'
    - from: npm run build
      to: dist/cli.js
      via: "tsc compilation"
    - from: npm run dev
      to: src/cli.ts
      via: "ts-node execution"
---

<objective>
Install core dependencies, create CLI entry point, configure build scripts, and set up code quality tools (ESLint/Prettier).

Purpose: Enable developer to build and run the TypeScript CLI with proper tooling for code quality.
Output: Working CLI with commander, build scripts, ESLint, and Prettier configuration.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-project-setup/01-01-SUMMARY.md
@.planning/PROJECT.md

Depends on 01-01: Foundation must be in place (package.json, tsconfig.json, folder structure).

Research decisions:
- CLI Framework: commander
- Add ESLint/Prettier in Phase 1 (not deferred)

Scripts to add:
- "build": "tsc"
- "dev": "ts-node src/cli.ts"
- "start": "node dist/cli.js"
- "clean": "rm -rf dist"
- "lint": "eslint src/"
- "format": "prettier --write src/"
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install core dependencies and CLI framework</name>
  <files>package.json</files>
  <action>
Install production dependencies:
- Run `npm install commander`

Install development dependencies:
- Run `npm install -D eslint prettier @typescript-eslint/parser @typescript-eslint/eslint-plugin`

Verify installations by checking package.json dependencies and devDependencies sections.
  </action>
  <verify>Run `cat package.json | grep -A5 '"dependencies"'` and verify commander is listed</verify>
  <done>commander installed as dependency, ESLint/Prettier packages installed as devDependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create CLI entry point with commander</name>
  <files>
    - src/index.ts
    - src/cli.ts
  </files>
  <action>
Create src/index.ts:
- Export main conversion function (placeholder/stub)
- Type: `export function convertStatement(pdfPath: string, outputPath: string): Promise<void>`
- For now, just log "Converting {pdfPath} to {outputPath}"

Create src/cli.ts:
- Import { Command } from 'commander'
- Create new Command instance
- Set program name, description, version
- Add required option: -i, --input <path> (PDF input file or directory)
- Add optional option: -o, --output <path> (Output Excel file, default: output/converted.xlsx)
- Add optional option: -v, --verbose (Enable verbose logging)
- Parse process.argv
- For now, log parsed options and call stub convert function

Make sure to use ES module syntax (import/export, not require/module.exports).
  </action>
  <verify>Run `cat src/cli.ts` and verify commander is imported and used correctly</verify>
  <done>src/index.ts and src/cli.ts exist with proper TypeScript syntax and commander setup</done>
</task>

<task type="auto">
  <name>Task 3: Configure npm scripts, ESLint, Prettier, and .gitignore</name>
  <files>
    - package.json
    - .gitignore
    - .prettierrc
    - eslint.config.mjs
  </files>
  <action>
Update package.json scripts section:
- "build": "tsc"
- "dev": "ts-node --esm src/cli.ts"
- "start": "node dist/cli.js"
- "clean": "rm -rf dist"
- "lint": "eslint src/"
- "format": "prettier --write src/"

Create .gitignore:
- dist/
- node_modules/
- *.log
- .env
- output/*.xlsx
- input/*.pdf (keep folder with .gitkeep, ignore actual PDFs)
- processed/*.pdf

Create .prettierrc:
- semi: true
- singleQuote: true
- trailingComma: "es5"
- tabWidth: 2
- printWidth: 100

Create eslint.config.mjs:
- Use @typescript-eslint/parser and @typescript-eslint/eslint-plugin
- Extend recommended TypeScript rules
- Configure for ES modules
- Set parserOptions.project to './tsconfig.json'
  </action>
  <verify>
Run `cat package.json | grep -A10 '"scripts"'` to verify scripts
Run `cat .gitignore` to verify ignore rules
Run `npm run lint -- --help` to verify ESLint works
  </verify>
  <done>package.json has all scripts, .gitignore excludes build artifacts, ESLint and Prettier configs exist</done>
</task>

<task type="auto">
  <name>Task 4: Verify build and dev scripts work</name>
  <files>
    - dist/
  </files>
  <action>
Test the TypeScript compilation:
1. Run `npm run build` - should compile without errors and create dist/ folder with .js files
2. Run `npm run dev -- --help` - should show CLI help using ts-node
3. Run `npm run dev -- -i input/test.pdf` - should execute with stub implementation

If any errors occur:
- Check tsconfig.json for correct module/moduleResolution settings
- Ensure all imports use .js extensions for ES modules (TypeScript requirement)
- Verify ts-node is configured for ES modules

Fix any TypeScript errors before marking complete.
  </action>
  <verify>
Run `npm run build` and check exit code is 0
Run `ls dist/` and verify .js files exist
Run `npm run dev -- --version` and verify CLI outputs version
  </verify>
  <done>TypeScript compiles without errors, npm run build creates dist/, npm run dev executes CLI successfully</done>
</task>

</tasks>

<verification>
- [ ] `npm install` completes without errors
- [ ] `npm run build` compiles TypeScript and creates dist/cli.js
- [ ] `npm run dev -- --help` shows CLI help
- [ ] `npm run dev -- -i input/test.pdf` runs without errors
- [ ] `npm run lint` runs without configuration errors
- [ ] `npm run format` runs without errors
- [ ] .gitignore excludes dist/, node_modules/, *.log
- [ ] src/cli.ts imports commander and parses arguments
</verification>

<success_criteria>
1. `npm run build` compiles TypeScript without errors, outputs to dist/
2. `npm run dev` executes CLI in development mode using ts-node
3. CLI accepts --input, --output, --verbose options via commander
4. ESLint and Prettier are configured and runnable via npm scripts
5. .gitignore properly excludes build artifacts and sensitive files
6. Developer can clone repo, run npm install, npm run build, npm run dev successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup/01-02-SUMMARY.md`
</output>
